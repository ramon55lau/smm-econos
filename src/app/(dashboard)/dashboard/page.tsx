"use client";

import UnifiedFlow from "@/components/flow/UnifiedFlow";

export default function DashboardHome() {
  return (
    <main style={{ minHeight: "100vh", background: "#f7f3f0" }}>
      <UnifiedFlow />
    </main>
  );
}
